pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        //can remove below ref from youtube
        maven {url 'https://jitpack.io'}
    }
}
rootProject.name = "Pujagoodies"
include ':app'
